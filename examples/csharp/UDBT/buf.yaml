version: v2
modules:
- path: proto
lint:
  use:
  - STANDARD
  except:
  - ENUM_VALUE_PREFIX
  - ENUM_ZERO_VALUE_SUFFIX
breaking:
  use:
  - FILE
deps:
- buf.build/googleapis/googleapis
- buf.build/grpc-ecosystem/grpc-gateway
